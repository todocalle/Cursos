var conformacion=prompt("Confirma la orden de compra", " Nada");

if(conformacion)
{
	alert ("El suario acepta la operacion" + conformacion);
}

else
{
	alert ("El usuario NO acepta la operacion" + conformacion);
}